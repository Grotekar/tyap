Sub Main()

        Dim s As String = "Привет"
        Dim a As Integer = 2
        Dim b As Integer = 5

        REM Посчитаем сумму
        Dim c As Integer = a + b

        REM Найти наибольшее
        Dim max As Integer
        If a > b Then
            max = a
        Else
            max = b
        End If

        REM Массив
        Dim mas As Integer() = {0, 1, 2, 3, 4, 5}

        c = a + mas(2)

        c = Fun(a, b)

    End Sub

    Function Fun(a As Integer, b As Integer) As Integer
        Return a + b
    End Function